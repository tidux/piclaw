- Audit structured logger payload consistency across the cleaned runtime/server scopes and add a metric for warn/error sites that still lack explicit operation/context fields; this could tighten the ticket's "module + operation context" acceptance criterion beyond raw-console elimination.
- Investigate whether `runtime/src/runtime/console-timestamps.ts` can be replaced with a lower-level stdout/stderr timestamping path or otherwise reduced, which would let the structured-logging audit drive `scope_allowlisted_console_calls` from 8 to 0 without reintroducing ad-hoc console usage.
- Consider a broader repo audit outside `runtime/src` (for example runtime scripts or selected web/server tooling) if the ticket needs stronger prevention against future raw-console regressions beyond the currently guarded runtime/server scopes.
